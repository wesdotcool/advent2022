package sample

import (
	"fmt"
	"io"
)

// for testing purposes
// go run advent.go sample
func Run(input io.Reader) (any, error) {
	var s string = "[N] [C] [D]"
	s2 := ""
	fmt.Println("", s[4] == '[')
	fmt.Println(len(s2))

	return nil, nil
}
